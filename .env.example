# Copy this file to ".env" and fill in your API keys.
# Both are free to obtain. The tool runs without them (extraction only),
# but enrichment requires the relevant key.

# https://www.virustotal.com/gui/my-apikey
VIRUSTOTAL_API_KEY=

# https://www.abuseipdb.com/account/api
ABUSEIPDB_API_KEY=
